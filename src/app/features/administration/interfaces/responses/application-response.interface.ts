export interface ApplicationResponse {
  id: number;
  clientId: string;
  name: string;
  description: string;
  launchUrl: string;
  isConfidential: boolean;
  isActive: boolean;
  createdAt: string;
}
